// Généré par docker-entrypoint.sh au démarrage du conteneur nginx à partir de la variable
// d'environnement BACKEND_URL (Dockploy) : ce fichier statique par défaut ne définit rien, pour
// que reassort-auth.js retombe sur son fallback (port :3001 du même hostname) en développement
// local, où ce fichier n'est jamais régénéré.
window.REASSORT_BACKEND_URL = null;
